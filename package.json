{
  "private": true,
  "name": "covid",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "export NODE_OPTIONS='--max-old-space-size=8192'; preact build --no-prerender",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "export NODE_OPTIONS='--max-old-space-size=8192'; preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-synacor": "^3.0.4",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "plotly.js": "^1.57.1",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2",
    "react": "github:preact-compat/react#1.0.0",
    "react-dom": "github:preact-compat/react-dom#1.0.0",
    "react-ssr-prepass": "npm:preact-ssr-prepass@^1.0.1",
    "react-plotly.js": "^2.5.0"
  }
}
